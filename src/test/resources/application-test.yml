spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: test-only-secret-not-for-production-min-32-bytes
    expiration-minutes: 60

logging:
  level:
    root: WARN
